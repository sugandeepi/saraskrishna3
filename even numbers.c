#include <stdio.h>
int main()
{ 
int a,b,i;
  printf("enter the 1st interval");
  scanf("%d",&a);
  printf("enter the 2nd interval");
  scanf("%d",&b);
  for(i=a;i<=b;i++)
  {
    if(i%2==0)
    {
      printf("%d is odd no is",i);
             }
             return 0;
             }
             }
